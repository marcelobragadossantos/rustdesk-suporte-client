name: Flutter Nightly Build

on:
  # nightly agendado desativado em 2026-06-21 (usamos build-suporte-windows.yml); manter apenas dispatch manual
  workflow_dispatch:

jobs:
  run-flutter-nightly-build:
    uses: ./.github/workflows/flutter-build.yml
    secrets: inherit
    with:
      upload-artifact: true
      upload-tag: "nightly"
